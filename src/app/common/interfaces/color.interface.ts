import { BaseEntity } from "src/app/admin/components/multi-select/multi-select.component";

export interface Color extends BaseEntity {
    // id?: number;
    // title: string;
    // value: string;  // only correct css color
}